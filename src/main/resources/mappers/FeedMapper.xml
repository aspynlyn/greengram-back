<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.wikibook.greengram.application.feed.FeedMapper">
    <select id="findAllLimitedTo">
        SELECT f.feed_id, f.contents, f.location, f.created_at, f.writer_user_id, u.uid, u.nick_name, u.pic
        FROM feed f
        JOIN user u
        ON f.writer_user_id = u.user_id
        LIMIT #{startIdx, #{size}
    </select>
</mapper>